.venv/
third_party/
__pycache__/
*.pyc
# Raw Codex exports: re-fetchable in ~1 min via scripts/fetch_data.sh
data/flywire_783/
runs/*
!runs/showcase/
!runs/life-showcase/
!runs/arcade/
# The working memory of whichever fly is currently alive: regenerated by every
# run, so it churns. runs/life-showcase/ preserves the trace that was measured.
life/
